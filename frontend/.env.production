# ─── API ─────────────────────────────────────────
# In production, point to your backend API URL.
# For same-origin deployment (Nginx reverse proxy), use: /api
# For separate domains, use: https://api.your-domain.com
VITE_API_URL=/api
